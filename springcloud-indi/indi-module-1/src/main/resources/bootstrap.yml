server:
  port: 8080
spring:
  application:
    #指定的应用名称： 对应 nacos Data Id
    name: springboot-demo
  cloud:
    nacos:
      discovery:
        server-addr: ip:port
      config:
        server-addr: ip:port
        group: local
        namespace: 8ed19181-8c92-4106-8c08-9ff13b175929
        #指定配置文件类型
        file-extension: yaml

  datasource:
    username: root
    password: mz666 #rewriteBatchedStatements=true
    url: jdbc:mysql://ip:port/chq_test?rewriteBatchedStatements=true&useUnicode=true&characterEncoding=utf-8&useSSL=false&serverTimezone=Asia/Shanghai
    driver-class-name: com.mysql.jdbc.Driver

  redis:
    host: ip
    port: port

mybatis-plus:
  global-config:
    db-config:
      id-type: auto
      field-strategy: not_empty
      column-underline: true
      db-type: mysql
    refresh: false
    logic-delete-value: 1
    logic-not-delete-value: 0
  configuration:
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl
    map-underscore-to-camel-case: true
    cache-enabled: false
  mapper-locations: classpath:mappers/*.xml

#熔断器隔离策略： Hystrix官方强烈建议使用THREAD
#THREAD以及SEMAPHORE，当隔离策略为 THREAD 时，是没办法拿到 ThreadLocal 中的值的。
#hystrix:
#  command:
#    default:
#      execution:
#        isolation:
#          strategy: Semaphore


xxl:
  job:
    admin:
      ### 调度中心部署地址：如调度中心集群部署存在多个地址则用逗号分隔。执行器将会使用该地址进行"执行器心跳注册"和"任务结果回调"
      addresses: http://10.80.73.167:8080/xxl-job-admin
      ### 调度中心通讯TOKEN，非空时启用
    accessToken:
    executor:
      ### 执行器"AppName"和地址信息配置
      appname: xxl-job-xfj
      address:
      ip:
      port: 1111
      # 执行器运行日志文件存储的磁盘位置，需要对该路径拥有读写权限
      logpath:
      # 执行器Log文件定期清理功能，指定日志保存天数，日志文件过期自动删除。限制至少保持3天，否则功能不生效
      logretentiondays: 30